name: CI - Mock Pokemon API Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Docker Compose
        run: |
          sudo curl -L "https://github.com/docker/compose/releases/download/v2.21.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          sudo chmod +x /usr/local/bin/docker-compose

      - name: Show Docker Compose version
        run: docker-compose --version

      - name: List running containers
        run: docker compose ps

      - name: Show BASE_URL environment variable
        run: echo "BASE_URL=$BASE_URL"

      - name: Bring up services
        run: docker compose up -d --build

      - name: Show Docker networks
        run: docker network ls

      - name: Inspect default network
        run: docker network inspect $(docker network ls -q -f name=python_api_pokemon_default)

      - name: Wait for services to be ready
        run: sleep 10  # Servislerin ayağa kalkması için bekle

      - name: Check running containers
        run: docker compose ps

      - name: Check mock-service hostname resolution
        run: docker compose run --rm api-tests sh -c 'ping -c 3 mock-service || echo "ping failed"'

      - name: Run tests inside api-tests container
        run: docker compose exec api-tests pytest tests/



      - name: Stop and remove containers
        run: docker-compose down

